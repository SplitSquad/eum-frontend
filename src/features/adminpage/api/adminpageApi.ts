import apiClient from './apiClient';
import {
  User,
  Post,
  PostDetail,
  Comment,
  CommentDetail,
  Reply,
  ReplyDetail,
  Report,
} from '../types';

/**
 * 관리자페이지 API 클래스
 */
class AdminpageApi {
  /**
   *
   */
}

// 단일 인스턴스 생성하여 내보내기
const adminpageApi = new AdminpageApi();
export default adminpageApi;
